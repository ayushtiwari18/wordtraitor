// This component is not being used and was added by mistake
// It can be integrated later when needed
// For now, the existing Results.jsx page handles game end

export default function GameOver() {
  return null
}